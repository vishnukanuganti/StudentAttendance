@model IEnumerable<StudentAttendance.Models.Course>

@{
    ViewData["Title"] = "Course Index";
}

<h1 class="text-primary">Index</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">
        <i class="fas fa-plus"></i> Create New
    </a>
</p>
<table class="table table-striped table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CourseName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CourseCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Credits)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CourseName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CourseCode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Credits)
                </td>
                <td>
                    <a class="btn btn-sm btn-warning" asp-action="Edit" asp-route-id="@item.CourseID">
                        <i class="fas fa-pencil-alt"></i> Edit
                    </a>
                    <a class="btn btn-sm btn-info" asp-action="Details" asp-route-id="@item.CourseID">
                        <i class="fas fa-info-circle"></i> Details
                    </a>
                    <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.CourseID">
                        <i class="fas fa-trash-alt"></i> Delete
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- Add these lines in your layout or head section to include Bootstrap and Font Awesome -->
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">

<!-- Add custom styles for background -->
<style>
    body {
        background-color: #f8f9fa;
    }
</style>
